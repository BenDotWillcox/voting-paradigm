# Consolidated Python requirements for Nebula Civitas.
#
# Organized by layer. Domain packages (voting/, preferences/) keep only what
# they themselves need; api/ pulls in the HTTP surface on top.

# --- Domain: numerical + scientific ---
numpy>=1.24.0
scipy>=1.11.0

# --- API: HTTP surface ---
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.0.0

# --- Testing ---
pytest>=8.0.0
httpx>=0.27.0
